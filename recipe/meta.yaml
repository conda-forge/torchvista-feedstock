{% set name = "torchvista" %}
{% set version = "0.2.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/torchvista-{{ version }}.tar.gz
  sha256: 45d26d391a423ad659be4440102a71f6daf0175ffdbfa52857729610896b86a9

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - ipython >=7.0.0
    - numpy >=1.18.0
    - pytorch

test:
  imports:
    - torchvista
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/sachinhosmani/torchvista
  summary: Interactive PyTorch model visualizer for notebooks
  license: GPL-3.0-only
  license_file:
    - LICENSE
    - NOTICE.txt

extra:
  recipe-maintainers:
    - sachinhosmani
