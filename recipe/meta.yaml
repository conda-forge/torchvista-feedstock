{% set name = "torchvista" %}
{% set version = "0.2.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/torchvista-{{ version }}.tar.gz
  sha256: 0f5b9710adc46825337d26f4d2aa49cf4de10bfb5812bcc445f36c523f0a5c73

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - ipython >=7.0.0
    - numpy >=1.18.0
    - pytorch

test:
  imports:
    - torchvista
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/sachinhosmani/torchvista
  summary: Interactive PyTorch model visualizer for notebooks
  license: GPL-3.0-only
  license_file:
    - LICENSE
    - NOTICE.txt

extra:
  recipe-maintainers:
    - sachinhosmani
